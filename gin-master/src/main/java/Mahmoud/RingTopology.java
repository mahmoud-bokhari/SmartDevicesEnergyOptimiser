package Mahmoud;

import DPO.OnDeviceExperiment;

public class RingTopology {
    // first pull current best
    // name it as deviceName-OS-BatteryLevel-SolutionSequence.csv
    // save it in Main_directory/DPO/deviceName/immigrants
    //NOTE unfinished code
    /*OnDeviceExperiment experiment = new OnDeviceExperiment();
                    for (String neighbour: OnDeviceExperiment.neighbours) {
                        utils.log("pulling immigrants from " + OnDeviceExperiment.device.deviceName, isPrintSteps);
                        String fileName = OnDeviceExperiment.device.deviceName+"-"+OnDeviceExperiment.device.OS+"-"+
                        OnDeviceExperiment.device.pullFromDevice(OnDeviceExperiment.DEFINE.concat("bestFound.txt"),
                                OnDeviceExperiment.immigrants_directory.concat(device.deviceName.concat("-").concat(device.OS))); // rename it
    }
    // second push to the device its neighbour's immigrants
    // Main_directory/DPO/deviceName/immigrants * here deviceName is one of the neighbours
    //

                    for (String neighbour: OnDeviceExperiment.neighbours) {
        utils.log("pushing immigrants from "+neighbour+" to "+OnDeviceExperiment.device.deviceName,isPrintSteps);
        String source = OnDeviceExperiment.immigrants_directory.concat();
        OnDeviceExperiment.device.pushToDevice(source,OnDeviceExperiment.DEFINE.concat("immigrants/"));
    }*/
}
